# 要件定義書

**プロジェクト名:** はとナビ おつかいクエスト  
**バージョン:** 1.0（デモプロトタイプ）  
**作成日:** 2026-06-20  
**技術スタック:** Dart / Flutter Web → Vercel

---

## 1. プロジェクト概要

子どもが保護者と一緒にスーパーマーケットでおつかいをするとき、  
安全に移動しながら商品を探し、食育クイズに答えてポイントを貯める  
**冒険型おつかい体験アプリ**。

対象ユーザー：小学校低学年の子ども（操作）と保護者（準備・精算）  
対象店舗イメージ：平和堂（琵琶湖・滋賀テーマの食育コンテンツ）

### 1-1. プロダクトの位置づけ

鳩ナビは、ピピットセルフを置き換えるものではなく、将来的にピピットセルフの買い物体験と接続可能な親子向け体験プロトタイプです。現時点では独立したWebデモとして動作し、将来的にはピピットセルフアプリ内統合、店舗貸出端末への搭載、または親のスマホでの買い物前後の補助利用を検討します。

- **現時点** ＝ 独立した Web デモとして単独動作する。
- **将来** ＝ ピピットセルフの買い物体験との接続を構想する。

### 1-2. 想定する3つの運用パターン

| パターン | 概要 | 位置づけ |
|---------|------|---------|
| A | ピピットセルフアプリ内に子ども向けモードとして統合する | 理想・将来構想 |
| B | 店舗貸出端末に搭載する（親はピピットセルフで会計、子は鳩ナビ） | 現実的 |
| C | 親のスマホで買い物前後の補助として使う（現デモに近い） | 両アプリ同時切替運用は非現実的 |

---

## 2. ビジネス要件

| ID | 要件 | 優先度 |
|----|------|--------|
| BR-01 | 子どもが単独で操作でき、おつかいを完遂できること | 必須 |
| BR-02 | 歩きスマホ・走り回りを防止する安全設計を持つこと | 必須 |
| BR-03 | 食育・地産地消を楽しみながら学べること | 必須 |
| BR-04 | ポイント→シール交換でリピート購買意欲を高めること | 必須 |
| BR-05 | デモとして Web ブラウザで動作し、Vercel から配信できること | 必須 |
| BR-06 | 将来的に Gemini API と連携してクイズ生成・巡回順の提案ができること | 追加 |
| BR-07 | 実際の JAN コードスキャンに対応すること | 追加 |

---

## 2-1. AI が行うこと / 行わないこと

AI（Gemini）は「考える補助」だけを担い、ナビの制御はすべてローカル（アプリ内ロジック）で行う。

| 区分 | 内容 |
|------|------|
| **AI が行うこと** | ① 巡回順の提案（回る順番だけを決める）／② 食育クイズの生成（将来：適応難易度・会話/解説・学びサマリ） |
| **AI が行わないこと** | 現在地推定／通路ルート生成／方角計算／危険判定／画面遷移／スキャン処理／精算（これらはローカル制御または対象外） |

※ 鳩ナビは会計・商品登録・精算を実装しない（ピピットセルフの役割）。鳩ナビのスキャンは「みつけた！」というゲームの合図であり、決済処理ではない。

---

## 3. 機能要件

### 3-1. ホーム画面
- 「クエストをはじめる」ボタンで買い物フローを開始する
- 「ポイント交換」ボタンで直接シール交換フローへ遷移する（将来）

### 3-2. 買い物リスト画面（親ターン）
- 商品リストをタップしてチェック・アンチェックできる
- 検索ボックスは UI として存在するが機能しない（ダミー）
- 1件以上選択しないと次へ進めない
- 「おつかいの順番を決める」ボタンで擬似ローディングを経て遷移する（AI＝巡回順の提案。物理ルートは生成しない）

### 3-3. お約束画面（引き渡し）
- 「はしらない」「まえをみる」の約束をタップして確認する
- 確認後にスマホを子どもへ渡すよう案内する
- **【追加実装】** この画面を新規作成する必要がある

### 3-4. 方位磁針画面（子どもターン・移動中）
- デバイスの方位センサー（DeviceOrientationEvent）を使い、コンパスを表示する
- 画面下部に「ゆっくりあるこう」を常時表示する
- 移動中は画面ロック（歩きスマホ防止）を演出する
- **【追加実装】** 現状は `navigation_screen.dart` 内のロック演出のみ。独立した compass 画面を追加する

### 3-5. 危険エリア警告
- 事前登録された危険箇所に近づくと警告ポップアップを表示する
- ポップアップ表示中は画面を赤くフラッシュする
- 現在はデモ用の固定文言。将来はマップ連動で自動判定する

### 3-6. 棚エリア到着・ミッション表示
- 次に向かう棚エリアを 1 つずつ表示する
- 「棚エリアに到着！」ボタンで次のフェーズへ進む
- 進捗バー（何件目 / 全件数）を表示する

### 3-7. JAN コードスキャン画面
- 「商品をスキャンする」ボタンで擬似スキャン演出を行う（現状）
- **【追加実装★】** `flutter_barcode_scanner` 等のパッケージで実際の JAN コードを読み取り、商品 ID と照合する

### 3-8. クイズ画面
- スキャンした商品に対応した 4 択クイズを表示する
- 正解すると 1P 獲得・限定バッジをアンロックする
- はとっぴーキャラクターが問題・解説を話す演出をつける
- **【追加実装★】** Gemini API で商品情報からクイズを自動生成する

### 3-9. 完了・ポイント確認画面
- 全商品を回ったら「おつかい大成功！」を表示する
- 「大人にスマホを返してね」の案内を表示する
- 今回獲得ポイントと累計ポイントを表示する
- 累計ポイントが交換基準を超えたらシール交換ボタンを活性化する

### 3-10. シール引換券表示
- 疑似 QR コードを表示し、サービスカウンターで提示できる画面を表示する

### 3-11. おつかい商品（8件）
- おつかいリストおよびクイズで用いる商品は以下の8件とする（実在商品名は使わない）。
- 各商品は売り場（`areaId`）に紐づき、巡回順の提案・逐次ナビの対象となる。

| id | 表示名 | areaId | 売り場 |
|----|--------|--------|--------|
| vegetables | みずうみ農園のトマト | local_vegetables | 地場野菜 |
| milk | あおぞら牧場のぎゅうにゅう | dairy | 乳製品 |
| egg | やまびこ農園のたまご | egg | 卵 |
| yogurt | あおぞら牧場のヨーグルト | yogurt | ヨーグルト |
| bread | こむぎ工房のパン | bread | パン |
| meat | みのり牧場のおにく | meat | お肉 |
| fish | みなと水産のおさかな | fish | お魚 |
| fruits | ひだまり農園のくだもの | fruits | 果物 |

---

## 4. 非機能要件

| カテゴリ | 要件 |
|---------|------|
| **デザイン** | ミントグリーン・グリーン基調。ボタンは角丸・太め・ポップ |
| **フォント** | 子どもが読みやすいひらがな中心のテキスト |
| **レスポンシブ** | スマートフォン縦向き（375px〜428px 幅）を主対象とする |
| **パフォーマンス** | Vercel CDN 配信により 3G 回線でも 3 秒以内に初期表示する |
| **安全性** | HTTPS 必須（センサー・カメラ API のため）。COOP/COEP ヘッダーを設定する |
| **オフライン** | 商品データ・クイズデータはアプリにバンドルし、ネット不要で動作する |
| **プライバシー** | 子どもの位置情報・カメラ映像をサーバーへ送信しない（将来も） |
| **アクセシビリティ** | 最小タップ領域 48×48 dp。フォントサイズ 14pt 以上 |

---

## 5. 制約事項

- Web（Flutter Web）で動作すること。ネイティブアプリのビルドは対象外（デモ段階）
- Gemini API は `gemini-2.5-flash` を使用し、API キーは Vercel 環境変数で秘匿してフロントに露出しない
- JAN コードスキャンは Web 環境のカメラ権限が必要なため、HTTPS 必須
- 方位センサーは iOS Safari で `DeviceOrientationEvent.requestPermission()` が必要

---

## 6. 将来拡張ロードマップ

```
Phase 1（現在）：擬似デモとして Vercel 配信
Phase 2（次期）：方位センサー連携・シール交換ポイント永続化
Phase 3（将来）：JAN コード実スキャン・Gemini API クイズ自動生成
Phase 4（発展）：店内マップ連動・危険箇所自動検出・マルチ店舗対応
```
