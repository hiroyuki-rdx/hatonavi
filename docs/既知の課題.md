# 既知の課題（バグ修正バックログ）

> 全機能の実装が一通り終わったあとに対応する項目。

| # | 症状 | 推定原因 | 対応方針 | 優先度 |
|---|------|---------|---------|--------|
| 1 | 危険エリアの**音声が鳴らない**（`speech_service.dart`） | ① iOS Safari は `speechSynthesis.speak()` を**ユーザー操作（タップ）起点**でしか鳴らせないが、現在は危険ダイアログを `addPostFrameCallback`（プログラム起点）で出しているため抑止される。② 音声(voices)の非同期ロード未完了。③ iPhoneのサイレント（消音）スイッチ。 | (a) 初回のお約束画面タップ時に**無音の `speak('')` で音声を解放（unlock）**しておく。(b) `voiceschanged` を待ってから日本語voiceを選択。(c) 危険検知を「到着ボタン押下＝ユーザー操作」と紐づけて鳴らす。(d) 画面のビープ音（HTMLAudioElement）併用も検討。 | 全実装後に対応 |

<!-- 以降、見つかった課題を追記していく -->
